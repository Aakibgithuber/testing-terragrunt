name: Deploy Infrastructure with Terragrunt

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      TG_PARALLELISM: 4
      AWS_REGION: ${{ secrets.AWS_REGION }}
      TF_VAR_S3_BUCKET_NAME: "github-navirego-dev-sftp-dump12122"
      TF_VAR_SFTP_PASS: "o3gnBzJPL1P0KHgQb051uVtIrHvCq6N0tNJ23bEpSmGzD"
      TF_VAR_DB_PASSWORD: "Admin1234!" # will take from github env variables after testing

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.5

      - name: Install Terragrunt
        run: |
          wget -qO- https://github.com/gruntwork-io/terragrunt/releases/download/v0.67.4/terragrunt_linux_amd64 > terragrunt
          chmod +x terragrunt
          sudo mv terragrunt /usr/local/bin/

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Run Terragrunt plan (dev)
        working-directory: ./live/dev
        run: |
          echo "🧩 Using TF_VAR_S3_BUCKET_NAME=$TF_VAR_S3_BUCKET_NAME"
          echo "🧩 Using TF_VAR_SFTP_PASS=$TF_VAR_SFTP_PASS"
          echo "🧩 Using TF_VAR_DB_PASSWORD=$TF_VAR_DB_PASSWORD"
          terragrunt run-all plan
          terragrunt run-all apply -auto-approve